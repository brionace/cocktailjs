# @smartender/glass-ui

Small utility package for cocktail glasses.

Usage (node):

import { GlassIcon, normalizeGlass } from '@cocktailjs;

const svg = GlassIcon({ glass: 'ShotGlass', size: 48 });
console.log(svg);
