<svg xmlns="http://www.w3.org/2000/svg"
      width=""
      height=""
      viewBox="0 0 64 90"
      preserveAspectRatio="xMidYMax meet"
    >
      <g>
        <Liquid
          d="" 12
            L ${cx + 11} 12
            L ${cx + 9} 38
            Q ${cx} 42 ${cx - 9} 38
            Z
          `}
          transform="scale(0.98)"
          idBase=""
          opacity=""
          liquidFill=""
        />
        {/* Original bowl liquid (commented out)
        <path
          d="" 12
            L ${cx + 11} 12
            L ${cx + 9} 38
            Q ${cx} 42 ${cx - 9} 38
            Z
          `}
          transform="scale(0.98)"
          class="liquid"
          opacity=""
          fill=""
        />
        */}

        {/* Bowl outline (stroke-only) */}
        <path
          d="" 12
            L ${cx + 11} 12
            L ${cx + 9} 38
            Q ${cx} 42 ${cx - 9} 38
            Z
          `}
          stroke="currentColor"
          stroke-width=""
          fill="none"
        />

        {/* Subtle highlight on the liquid surface */}
        <path
          d="" 18
            L ${cx + 8} 18
            L ${cx + 6} 30
            Q ${cx} 33 ${cx - 6} 30
            Z
          `}
          fill="rgba(255,255,255,0.06)"
        />

        {/* Stem */}
        <rect
          x=""
          y=""
          width=""
          height=""
          fill="currentColor"
          stroke="currentColor"
          stroke-width=""
        />

        {/* Base */}
        <path
          d="" 50 Q ${cx} 48 ${cx + 6} 50 L ${cx + 6} 50 L ${
            cx - 6
          } 50 Z`}
          fill="currentColor"
          stroke="currentColor"
          stroke-width=""
        />
      </g>
    </svg>